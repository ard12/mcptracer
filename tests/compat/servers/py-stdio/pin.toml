# Trusted tool contract for the real Python SDK server, pinned from a
# session recorded against the unmodified server (MCPTRACER_COMPAT_RUGPULL
# unset). Same bootstrap procedure as examples/rug-pull-demo/pin.toml: run
# `assert --spec` with an empty hashes table once against a trusted
# recording and copy the printed hash in.
[[assert]]
kind = "tools_pinned"
hashes = { send_email = "71b16153538d57492ff567acb21ef71dd253af2d690bd7d98d33df62e180ae6e" }
